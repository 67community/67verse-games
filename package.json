{
  "name": "67verse-game",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:skyway": "node scripts/local-skyway-server.mjs",
    "build": "vite build && node scripts/check-performance-budget.mjs",
    "build:unchecked": "vite build",
    "preview": "vite preview",
    "capture:movement-qa": "node scripts/capture-movement-qa.mjs",
    "capture:visual-qa": "node scripts/capture-visual-qa.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node --test tests/browser-smoke.mjs",
    "test:all": "npm test && npm run test:browser"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "puppeteer-core": "^25.3.0",
    "vite": "^8.1.5",
    "ws": "^8.21.1"
  }
}
