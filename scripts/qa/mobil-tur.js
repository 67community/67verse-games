// mobil-tur.js — the phone-screen walkthrough: entry, version choice, picker,
// hub with touch controls, the phone. Shots at 2.5 / 5 / 8 / 12 / 15s.
(async () => {
  const tikla = (sec) => document.querySelector(sec)?.click();
  setTimeout(() => tikla('.ks-kart[data-surum="nft"]'), 3300);
  setTimeout(() => tikla('#ks-sec'), 6300);
  setTimeout(() => tikla('#tf-dugme'), 10500);
  return 'mobil tur armed';
})()
